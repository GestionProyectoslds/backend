namespace GDP_API
{
    /// <summary>
    /// Represents the priority levels for an activity on a 1-3 scale. From  low to  high. 
    /// </summary>
    public enum ProjectPriority
    {
        Low = 1,
        Medium = 2,
        High = 3,
        Highest = 4
    }
}