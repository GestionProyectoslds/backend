namespace GDP_API
{
    /// <summary>
    /// Represents the priority levels for an activity on a 1-5 scale. From very low to very high. 
    /// </summary>
    public enum ActivityPriority
    {

        VeryLow = 1,
        Low = 2,
        Normal = 3,
        High = 4,
        VeryHigh = 5,
    }
}